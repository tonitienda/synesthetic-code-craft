import {makeProject} from '@motion-canvas/core';

import backpropagationAct1 from '../scenes/backpropagationAct1?scene';

export default makeProject({
  name: 'backpropagation',
  scenes: [backpropagationAct1],
});
