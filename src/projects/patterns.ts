import {makeProject} from '@motion-canvas/core';

import commandPhraseLift from '../scenes/patternCommandPhraseLift?scene';

export default makeProject({
  name: 'patterns',
  scenes: [commandPhraseLift],
});
